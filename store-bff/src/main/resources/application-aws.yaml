# test if this is really needed:
server:
  forward-headers-strategy: native

spring:
  config:
    import:
      - aws-secretsmanager:client-secrets

  security:
    oauth2:
      client:
        registration:
          store-client-oidc:
            provider: spring
            client-id: store-client
            client-secret: ${bff-client}
            authorization-grant-type: authorization_code
            redirect-uri: "https://pinkkstore.com/login/oauth2/code/{registrationId}"
            scope: openid,profile
            client-name: store-client-oidc
          store-client-authorization-code:
            provider: spring
            client-id: store-client
            client-secret: ${bff-client}
            authorization-grant-type: authorization_code
            redirect-uri: "https://pinkkstore.com/authorized"
            scope: openid,profile
            client-name: store-client-authorization-code
        provider:
          spring:
            issuer-uri: https://auth.pinkkstore.com

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: userinfo
              uri: https://auth.pinkkstore.com
              predicates:
                - Path=/userinfo
              filters:
                - TokenRelay=
            - id: resource-server
              uri: https://api.pinkkstore.com
              predicates:
                - Path=/api/**
              filters:
                - TokenRelay=
                - StripPrefix=1

app:
  base-uri: https://pinkkstore.com
  authentication-entry-point: https://pinkkstore.com/oauth2/authorization/store-client-oidc
